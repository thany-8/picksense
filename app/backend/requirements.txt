# Web API
fastapi==0.116.1
python-multipart==0.0.20
uvicorn[standard]==0.35.0

# Inference runtime (imported by model_utils.py). Left unpinned because the
# correct torch/torchvision build depends on your platform and CPU/GPU (CUDA)
# setup; see the repository README for the reference install.
torch
torchvision
pillow